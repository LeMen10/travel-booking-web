package WebSpring.Controllers.User;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import WebSpring.Model.Customer;

import org.springframework.stereotype.Controller;

public class PaymentController {

//	@GetMapping("/payment")
//    public ResponseEntity<?> listAllCustomers() {
//        List<payments> listPayment = paymentRespository.findAll();
//        if (listPayment.isEmpty()) {
//            return new ResponseEntity<>("No payment found", HttpStatus.OK);  // Return a message if the list is empty
//        }
//        return new ResponseEntity<>(listPayment, HttpStatus.OK);
//    }
}



